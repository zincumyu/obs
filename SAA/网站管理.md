---
tags:
  - 项目
  - SAA
---

#开发 [[Bug]] [[SAA项目清单]]
```
sudo systemctl daemon-reload
sudo systemctl start saa-api
sudo systemctl status saa-api
sudo systemctl restart saa-api
```

没有写网站拉取gitnore
需要重写.env

pip install -r requirements.txt
# 1. 安装 Node.js（如果没装）
node --version || curl -fsSL https://deb.nodesource.com/setup_20.x | sudo bash - && sudo apt install -y nodejs

# 2. 安装 Chromium 浏览器
sudo apt install -y chromium-browser

# 3. 安装 puppeteer 依赖
cd /path/to/SAA_0/test/puppeteer_word_pdf
npm install

# 4. 拉取最新代码并重启
git pull
# 重启 FastAPI 服务

git reset --hard origin/main

echo '{"title":"test","words":[],"materials":[],"corrections":[{"id":1,"title":"test","data":{"score":{"total":20},"review":{"overall":"ok"}},"created_at":"2026-07-25"}]}' > /tmp/test.json
node generate_notebook.js /tmp/test.json /tmp/test.pdf

echo "=== 服务状态 ===" && sudo systemctl status saa-api 2>&1 | head -20 && echo "" && echo "=== 最近50条日志 ===" && sudo journalctl -u saa-api --no-pager -n 50 2>&1 && echo "" && echo "=== 端口占用 ===" && sudo ss -tlnp 2>/dev/null | grep -E ":(80|443|8080|8000|5000|3000)" && echo "" && echo "=== Nginx状态 ===" && sudo nginx -t 2>&1

服务器装中文 emoji
yum install -y wqy-microhei-fonts
yum install -y google-noto-emoji-fonts


## 镜像



 坑 1：npm 官方源在国内慢 → 换镜像，这一条能把时间砍到 1/3

```
npm config set registry https://registry.npmmirror.com
```
坑 2：profile 依赖走 pnpm → 也给它配上镜像
```bah
pnpm config set registry https://registry.npmmirror.com
```
