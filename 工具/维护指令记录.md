---
tags:
  - 工具
  - hermes
  - 指令
---

# 维护指令记录

> 这里登记在对话里用到的开发和维护指令——换模型、加密钥、改配置这类。
> kael 负责往下记，一条一行，按时间排。

## 2026-09-20

| 指令 | 干什么 | 备注 |
| --- | --- | --- |
| `hermes auth add zai` | 把 GLM（z.ai）的密钥加进凭据池 | 在终端里交互输入，不回显、不进 history、不进聊天 |
| `hermes config set model.provider zai` | 把推理 provider 切成 GLM | — |
| `hermes config set model.default glm-5.2` | 默认模型换成 glm-5.2 | — |
| `hermes config set model.base_url ""` | 清掉原来指向 deepseek 的 base_url | 不然后面切到 GLM 还在往 deepseek 的地址发 |
| `hermes config set model.default glm-5.3-flash` | 换成 GLM 的 flash（快一些） | — |
| `hermes config set model.provider deepseek` | 切回 deepseek | — |
| `hermes config set model.default deepseek-flash` | 默认模型切回 deepseek-flash | — |

### 相关的只读命令

| 指令 | 看什么 |
| --- | --- |
| `hermes config get model` | 当前 provider / 模型 / base_url |
| `hermes auth list` | 凭据池里有哪几家、各自几条 |
| `hermes auth status zai` | 某家登录状态 |
| `hermes fallback list` | 主模型挂了会切到谁 |
| `hermes doctor` | 体检 |

## 2026-09-21

| 指令 | 干什么 | 备注 |
| --- | --- | --- |
| 会话内切模型：glm-5.3-flash → deepseek-flash | 只切了当前会话 | `hermes config get model` 仍显示 zai / glm-5.3-flash，磁盘配置没动 |
| `hermes config set model.provider deepseek` | 主 provider 改回 deepseek | 原来 zai 的额度在限流（429） |
| `hermes config set model.default deepseek-flash` | 默认模型改回 deepseek-flash | `base_url` 留空，走 deepseek 插件自带的 `https://api.deepseek.com/v1` |
| `hermes cron edit 82e609f0eab0 --provider deepseek --model deepseek-flash` | 把「公益打卡催办」(9/25) 从 zai 搬到 deepseek | 任务带 provider/model snapshot，不搬就还往 zai 发 |
| `hermes cron edit a7e51ab7e353 --provider deepseek --model deepseek-flash` | 同上，「公益打卡最后期限」(9/27) | — |

### 课表订阅（2026-09-21）

| 指令 / 文件 | 干什么 | 备注 |
| --- | --- | --- |
| `.hermes/scripts/kebiao_ics.py` | 把 `kb/kebiao.json` 生成 `kb/kebiao.ics`（26 条） | 周次 `[n]` 语义没确认，先按每周算 |
| `sudo cp /tmp/kebiao-ical.service /etc/systemd/system/ && sudo systemctl enable --now kebiao-ical` | 把 .ics 挂成订阅地址的常驻服务 | 监听 0.0.0.0:8765，开机自启 |
| `kb/.ical_token` | 订阅路径里的随机码 | 路径 = `/k/<token>/kebiao.ics`，别的路径 404 |

**已通**：2026-09-22 13:14 有外网机器（111.55.147.96）从外面取到 200——他在轻量控制台放行了 TCP 8765。订阅地址 = `http://1.14.145.33:8765/k/<token>/kebiao.ics`（token 在 `kb/.ical_token`，每次改课表后内容自动跟着变，不用重新订阅）。


## 2026-09-25

| 指令 / 文件 | 干什么 | 备注 |
| --- | --- | --- |
| `.hermes/scripts/bili_digest.py` | B 站每日推荐：wbi 签名搜索（16 个技术关键词 × 2 页）+ 科技/知识区排行，按播放量为主打分，30 天内不重复，90 分钟以上的课跳过 | 不登录也能查；`kb/bili_seen.json` 存已推过的 bvid |
| cron `afd286bac14d`「B站每日推荐」`30 7,12 * * *` | 每天 7:30 / 12:30 各推 3 条到 QQ | `no_agent`，脚本直接出文；挑不出来就不发（空 stdout＝不推送） |

## 备注

- `hermes fallback add` 是交互式的选择器；要脚本化就直接写 `fallback_providers`（`hermes config set`）。
- 改模型不会动记忆和笔记——那些是磁盘上的文件。
- 换 provider 之后，之前建的定时任务还锁在旧的 provider/model 上（它们有 snapshot），要一起搬得单独处理。
